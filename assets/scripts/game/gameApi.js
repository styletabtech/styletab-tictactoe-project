'use strict';

// const app = require('../app');

const playerMove = (data) => {
  console.log(data); // to check if this is working
  // return $.ajax({
  //   url: app.host + '/games',
  //   method: 'POST',
  //   data: data,
  // });
};


module.exports = {
playerMove,

};
